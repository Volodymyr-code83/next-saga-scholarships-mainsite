/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['saga-scholarship.vercel.app'],
    },
    // Uncomment if needed
    output: "export",
};

export default nextConfig;
